{
  "name": "@attivio/fetchutils",
  "version": "0.0.1",
  "description": "Wrapper around the fetch() API to communicate with the Attivio REST APIs when authenticated with SAML.",
  "main": "./dist/index.js",
  "scripts": {
    "init": "mkdir dist",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean && npm run init",
    "build": "babel ./src -d ./dist --ignore test.js"
  },
  "author": "Attivio, Inc.",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.17.0",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-preset-es2015": "^6.16.0",
    "flow-bin": "^0.79.1"
  },
  "files": [
    "dist"
  ]
}
